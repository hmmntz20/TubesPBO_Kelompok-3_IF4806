# ──────────────────────────────────────────────────────────────────────────
# Telkom Route Finder — backend production overrides (profil: prod)
#
# Diaktifkan via SPRING_PROFILES_ACTIVE=prod atau --spring.profiles.active=prod.
# Datasource tetap dibaca dari env var (lihat application.properties).
# Tambahkan di sini hanya properti yang berbeda dari default (mis. logging,
# tuning Hikari, monitoring, rate-limit) saat fase release.
# ──────────────────────────────────────────────────────────────────────────

# Contoh (di-comment hingga dipakai):
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.show-sql=false
# logging.level.root=WARN
# server.compression.enabled=true
